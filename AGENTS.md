# lez-bonding-curve-launchpad

Proof of concept for [Logos RFP-015](https://github.com/logos-co/rfp/blob/master/RFPs/RFP-015-bonding-curve-launchpad.md).
Our proposal is [logos-co/rfp#118](https://github.com/logos-co/rfp/issues/118).

## Agent skills

### Issue tracker

Linear, GTM team, default project "LEZ Bonding Curve Launchpad PoC". See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical labels exist verbatim in Linear; the native Triage status also reads as `needs-triage`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Scaffold (lgs) skills

Installed by `logos-scaffold init`. Regenerated on re-run, so keep this section's
prose out of the generated block and re-merge if `lgs init` runs again.

This is a `default`-template project, but `runner_support` no longer lives in a root
`src/lib.rs` — it moved to `crates/launchpad-client`. The `lez-template` skill lists that
file as one of its identification criteria, so identify the template from `scaffold.toml`
and `methods/guest/src/bin/` instead. See `docs/adr/0002`.

This project ships skills for AI coding assistants. They are regenerated by
`logos-scaffold new` and `logos-scaffold init`; commit them so your team gets
the same guidance.

- **Claude Code** reads `.claude/skills/<name>/SKILL.md` directly.
- **Cursor** reads `.cursor/rules/<name>.mdc`.
- **Codex / other agents** can open the matching skill below when its trigger applies.

| Skill | When it applies |
|---|---|
| [`basecamp`](.claude/skills/basecamp/SKILL.md) | Use when running any `lgs basecamp …` subcommand or working on a Logos module project (a project that builds `.lgx` artefacts via `flake.nix#packages.<system>.lgx`). Covers `setup` / `modules` / `install` / `launch` / `develop` / `build` / `build-portable` / `run` / `doctor` / `paths` / `docs`, the `[modules]` schema, per-profile XDG isolation under `.scaffold/basecamp/profiles/{alice,bob}/`, clean-slate launch semantics, two-instance p2p dogfooding, and AppImage-targeted portable builds. Activates additionally on presence of `[modules]` in `scaffold.toml` or `.scaffold/basecamp/profiles/`. Independent of template skills. |
| [`lez-framework-template`](.claude/skills/lez-framework-template/SKILL.md) | Use when working inside a project scaffolded with scaffold's lez-framework template — Anchor-on-Solana parallel for LEZ with #[lez_program] / #[instruction] / #[account(...)] macros and auto-generated IDL. Identify by scaffold.toml framework = "lez-framework", crates/lez-client-gen/, idl/, and #[lez_program] in the source. |
| [`lez-template`](.claude/skills/lez-template/SKILL.md) | Use when working inside a project scaffolded with the bare LEZ template (`lgs new` default — raw Rust + risc0 guest programs, no framework macros). Identify by scaffold.toml without `framework = "lez-framework"`, presence of `methods/guest/src/bin/*.rs`, and absence of `idl/` + `crates/lez-client-gen/`. |
| [`lgs-cli`](.claude/skills/lgs-cli/SKILL.md) | Use for the `lgs` / `logos-scaffold` CLI as a whole — bootstrap a project, run setup/build/deploy/localnet/wallet/doctor/report, diagnose CLI errors, or adopt scaffold in an existing project. Covers the full CLI surface, the `.scaffold/` state layout, and error → recovery patterns. Entry point that routes into `lez-template`, `lez-framework-template`, or `basecamp` once project context is identified. |

Open the matching skill file when its activation criteria apply.
